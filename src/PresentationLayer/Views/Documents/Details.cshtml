@model DocumentDetailsDto
@{
    ViewData["Title"] = "Document Details";
}

<div class="row g-4">
    <div class="col-lg-4">
        <div class="card app-card">
            <div class="card-body">
                <h5 class="card-title">@Model.OriginalFileName</h5>
                <dl class="small mb-0">
                    <dt>Course</dt>
                    <dd>@Model.CourseCode - @Model.CourseName</dd>
                    <dt>Chapter</dt>
                    <dd>@Model.ChapterTitle</dd>
                    <dt>File</dt>
                    <dd>@Model.FileType / @(Model.FileSizeBytes / 1024) KB</dd>
                    <dt>Content Hash</dt>
                    <dd class="text-break">@Model.ContentHash</dd>
                    <dt>Uploaded By</dt>
                    <dd>@(Model.UploadedByEmail ?? "Unknown")</dd>
                    <dt>Uploaded At</dt>
                    <dd>@Model.UploadedAtUtc.ToString("yyyy-MM-dd HH:mm") UTC</dd>
                    <dt>Sections</dt>
                    <dd>@Model.Chunks.Count</dd>
                    <dt>Chunking</dt>
                    <dd><span class="badge text-bg-light">@Model.ChunkingStrategy</span></dd>
                </dl>
                <a class="btn btn-outline-secondary mt-3" asp-controller="Documents" asp-action="Index">Back</a>
            </div>
        </div>
    </div>
    <div class="col-lg-8">
        <div class="card app-card mb-4">
            <div class="card-body">
                <h5 class="card-title">Document Text</h5>
                <pre class="document-text">@Model.ContentText</pre>
            </div>
        </div>
        <div class="card app-card">
            <div class="card-body">
                <h5 class="card-title">Indexed Sections</h5>
                <div class="table-responsive">
                    <table class="table table-sm align-middle">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>Content</th>
                            <th>Retrieval</th>
                        </tr>
                        </thead>
                        <tbody>
                        @foreach (var chunk in Model.Chunks)
                        {
                            <tr>
                                <td>@chunk.ChunkIndex</td>
                                <td class="small">@chunk.Content</td>
                                <td class="small">
                                    @(chunk.EmbeddingModels.Count == 0
                                        ? "Missing embedding"
                                        : $"Vector: {string.Join(", ", chunk.EmbeddingModels)}")
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
