@{
    Layout = "_ReferenceLayout";
    ViewData["Title"] = "Document detail reference";
    var state = ViewData["State"]?.ToString() ?? "ready";
}

<div class="detail-reference">
    <nav class="breadcrumb-row" aria-label="Breadcrumb"><a asp-action="Documents">Documents</a><span>/</span><span>Dependency Injection Notes.pdf</span></nav>
    <header class="detail-heading"><div><p class="lab-eyebrow">Phase 15 · Source detail</p><h1 data-live-document-title>Dependency Injection Notes.pdf</h1><p data-live-document-meta>PRN222 · Chapter 04 · PDF · 1.8 MB</p></div><span class="ui-badge @(state == "failed" ? "ui-badge-danger" : state == "processing" ? "ui-badge-warning" : "ui-badge-success")"><i></i>@(state == "failed" ? "Indexing failed" : state == "processing" ? "Processing 68%" : "Indexed")</span></header>
    @if (state == "failed") { <div class="detail-error" role="alert"><strong>Embedding generation stopped.</strong><span>The extracted text and chunks are safe. Check the Hugging Face configuration, then retry indexing.</span><button class="ui-button ui-button-secondary">Retry indexing</button></div> }
    <div class="detail-grid">
        <main class="detail-main">
            <div class="ui-tabs" role="tablist" aria-label="Document detail"><button role="tab" aria-selected="true" data-tab-target="overview">Overview</button><button role="tab" aria-selected="false" data-tab-target="extracted">Extracted text</button><button role="tab" aria-selected="false" data-tab-target="detail-chunks">24 chunks</button></div>
            <section class="ui-tab-panel" id="overview" role="tabpanel"><div class="document-summary" data-live-document-summary><h2>Document summary</h2><p>This lecture note introduces dependency injection as a framework service, explains the transient, scoped, and singleton lifetimes, and shows constructor injection in MVC controllers.</p><dl><div><dt>Characters</dt><dd>28,416</dd></div><div><dt>Sections</dt><dd>12</dd></div><div><dt>Chunking</dt><dd>Paragraph</dd></div><div><dt>Embedding</dt><dd>multilingual-e5-base</dd></div></dl></div></section>
            <section class="ui-tab-panel" id="extracted" role="tabpanel" hidden><article class="extracted-copy" data-live-extracted><h2>4. Dependency Injection</h2><p>Dependency injection is a design pattern in which an object receives the services it depends on. ASP.NET Core includes a built-in service container. Services are registered during application startup and requested through constructors.</p><p>A scoped service is created once per HTTP request. This lifetime is appropriate for application services and data access boundaries that belong to one request.</p><h3>Constructor injection</h3><p>Controllers declare required services in their constructors. The framework resolves those services before invoking the action.</p></article></section>
            <section class="ui-tab-panel" id="detail-chunks" role="tabpanel" hidden><div class="chunk-list" data-live-document-chunks><article><span>#01</span><div><strong>Dependency injection overview</strong><p>Dependency injection is a design pattern in which an object receives...</p></div><small>1,204 chars</small></article><article><span>#02</span><div><strong>Service lifetimes</strong><p>Transient services are created each time they are requested...</p></div><small>986 chars</small></article><article><span>#03</span><div><strong>Constructor injection</strong><p>Controllers declare required services in their constructors...</p></div><small>1,118 chars</small></article></div></section>
        </main>
        <aside class="index-timeline"><p class="lab-eyebrow">Indexing timeline</p><ol><li class="done"><strong>Uploaded</strong><small>10:42:02</small></li><li class="done"><strong>Text extracted</strong><small>10:42:05</small></li><li class="done"><strong>24 chunks saved</strong><small>10:42:06</small></li><li class="@(state == "failed" ? "failed" : state == "processing" ? "active" : "done")"><strong>Embeddings created</strong><small>@(state == "failed" ? "Failed" : state == "processing" ? "68% complete" : "10:42:18")</small></li><li class="@(state == "ready" ? "done" : "")"><strong>Finalized</strong><small>@(state == "ready" ? "10:42:19" : "Waiting")</small></li></ol></aside>
    </div>
</div>
